# Codebase research (smoke-unit)

## How it works today

- `src/counter.py` exposes `increment(value, step=1)` and a `main()` that increments thrice from 0.
- No clamp, no CLI flags, no tests in the seed.

## Edit sites

- `src/counter.py` — all remaining plan tasks

## Discard if

Harness seed is regenerated with a different toy surface.
