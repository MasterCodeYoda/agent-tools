# Smoke counter (workflow harness seed)

Tiny toy project used only by `tests/workflow` scenarios. Not a product.

```bash
python -m src.counter
```

Planning unit: `.agent-tools/planning/smoke-unit/`.
