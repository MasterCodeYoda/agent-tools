"""Deterministic tooling for the /swarm test harness.

`generate` builds a throwaway git repo from a scenario; `ingest` turns a run's
session logs into structured observations. The agent-driven assessment lives in
the project-scoped `swarm:test` skill, not here.
"""
