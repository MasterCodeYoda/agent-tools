[project]
name = "agent-tools-python"
version = "0.1.0"
description = "Python tools for agent workflows"
requires-python = ">=3.12"
dependencies = ["opencv-python-headless>=4.9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"
