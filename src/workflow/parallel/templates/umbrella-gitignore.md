# .agent-tools/.gitignore entries

```
# Managed by Agent Tools. User edits respected on re-run.

# Parallel-mode transient state, managed by /workflow:setup (per-run; not project source)
parallel/active-run
parallel/sessions/
```
