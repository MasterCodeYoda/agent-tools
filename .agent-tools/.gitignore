# Managed by Agent Tools. User edits respected on re-run.

# Parallel-mode transient state (per-run; not project source)
parallel/active-run
parallel/sessions/

# Legacy swarm path names (if any residual)
swarm/active-run
swarm/sessions/
