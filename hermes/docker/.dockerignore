# context is repo root when: docker build -f hermes/docker/Dockerfile .
# This file must live next to Dockerfile OR we use root .dockerignore —
# docker only reads .dockerignore from context root. See repo-root .dockerignore.kevin
