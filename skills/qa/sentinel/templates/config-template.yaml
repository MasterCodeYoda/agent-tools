# ── Sentinel Configuration ───────────────────────────────────────────
# Generated by qa-sentinel:setup
# ──────────────────────────────────────────────────────────────────────

app:
  base_url: http://localhost:3000
  name: My Application

auth:
  strategy: none
  # strategy: credentials
  # login_url: /login
  # credentials:
  #   username: qa-tester@example.com
  #   password_env: SENTINEL_QA_PASSWORD
  # strategy: token
  # (Set SENTINEL_AUTH_TOKEN env var)
  # strategy: cookie
  # (Set SENTINEL_SESSION_COOKIE env var)

browser:
  headless: true
  viewport:
    width: 1280
    height: 720
  timeout: 30000

# Bridge configuration (for Tauri apps tested via HTTP bridge)
bridge:
  # shim_path: apps/http-bridge/bridge-inject.js
  # port: 9990
  # partitions:
  #   workspace-pages: 9990
  #   editor-content: 9991

# Playwright configuration
playwright:
  config_path: ./playwright.config.ts

# Spec and test directories
specs:
  nl_dir: ./specs
  tests_dir: ./tests
